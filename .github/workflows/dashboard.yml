name: Dashboard Summary

on:
  workflow_dispatch:
  push:
    branches: [main, master]
  pull_request:

jobs:
  dashboard-summary:
    if: false  # DISABLED: workflow issues under investigation
    uses: sorensencc-dotcom/.github/.github/workflows/dashboard-summary.yml@main
