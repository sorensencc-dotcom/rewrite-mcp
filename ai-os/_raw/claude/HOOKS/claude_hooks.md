# Claude Hooks

This document outlines the inferred structural definition of Hooks for the Claude platform.

---
-   **Source:** `UNKNOWN`
-   **Confidence:** `None`
---

## 1. Hook Architecture (Unknown)

-   There is no information available in the user's workspace or in the provided prompts to infer the existence or structure of a "Hook" system for the Claude platform.
-   It is unknown if Claude has a mechanism equivalent to Gemini's "Hook Context" for injecting informational data into its prompt.
-   The project's `rewrite-mcp` repository contains a `.husky/` directory with a `pre-commit` hook. While this is a standard Git hook and not specific to any AI, it is a form of "hook" present in the development workflow that affects all systems.
