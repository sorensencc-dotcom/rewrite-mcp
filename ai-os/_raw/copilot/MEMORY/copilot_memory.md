# Copilot Memory

This document outlines the inferred structural memory systems for the Microsoft Copilot platform, based on observations from the user's workspace.

---
-   **Source:** `INFERRED` (from `.github/copilot-instructions.md`)
-   **Confidence:** `Low`
---

## 1. Inferred Memory Mechanisms

Copilot's memory appears to be primarily focused on providing context for code completion and in-editor chat.

### 1.1. In-Editor Context (Volatile)

-   **Nature:** Copilot's primary "memory" is the content of the currently open files in the editor. This is a volatile, session-based memory used to provide relevant code suggestions.

### 1.2. `copilot-instructions.md` (Persistent Context)

-   **Location:** `.github/copilot-instructions.md`
-   **Nature:** This file serves as a form of persistent, project-specific memory. It provides instructions and context to Copilot.
-   **Content:** Based on the observed file, it is automatically generated by a "Copilot Toolbox" and contains discovered information about project skills and MCP servers. It is not intended for manual editing of preferences but rather as a dynamically updated context manifest.

## 2. Unknowns

-   The export prompt mentions "Microsoft Copilot (memory + preferences)", and the `copilot-instructions.md` file mentions "User skills" and "User MCP" being loaded from user-specific paths (`C:\Users\soren\...`). This strongly implies a persistent, user-level memory and preference system.
-   **The structure, format, and location of these user-level memory and preference files are unknown**, as they are not exposed within the project workspace.
