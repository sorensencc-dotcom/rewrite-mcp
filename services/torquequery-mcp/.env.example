# TorqueQuery MCP Server Configuration

# Substrate Service URL
SUBSTRATE_URL=http://localhost:3000

# MCP Server Port (if running as HTTP instead of stdio)
MCP_PORT=5000

# Node environment
NODE_ENV=development
