{
  "name": "image-analyzer-v2-tests",
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "vitest": "^1.0.0"
  },
  "scripts": {
    "test": "vitest --run --reporter=verbose",
    "test:watch": "vitest"
  }
}
