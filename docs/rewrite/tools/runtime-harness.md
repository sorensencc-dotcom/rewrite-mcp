# Runtime Stress Harness

Tool for performing concurrency stress tests against the CIC Intelligence server.

## Usage

```bash
cd tools/runtime-harness
npm install
npm run stress
```

## Configuration

Modify `harness.config.json` to adjust target endpoints, concurrency levels, and test duration.
